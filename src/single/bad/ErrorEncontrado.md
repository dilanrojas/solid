# Error
La clase UserDataManager se encarga tanto de almacenar el usuario, así como de validar sus datos.

La validación de los datos debería delegarse a otras clases separadas, así como el registro y el ingreso.
